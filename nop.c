#include "monty.h"
/**
 * nop - Doesn't do anything/ NULL statement
 * @stack: Double pointer passed in function
 * @line_number: variable passed in function
 * Return: 0
 */
void nop(stack_t **stack, unsigned int line_number)
{
stack = stack;
line_number = line_number;
}
